# CI verification

This branch exists only to trigger the repository's existing pull-request CI without changing `main`.

No application code, payment flow, database schema, or secrets are changed by this verification commit.
